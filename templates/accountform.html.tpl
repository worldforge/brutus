<div id="accountFormWrapper{$step}" class="account">	<form name="accountForm"		  action="/index.php?verb=Account"		  onsubmit="return submitAjaxAccountForm(this);">		<input type="hidden" name="step" value="{$step}" />		{if $step == 1}			<h1>Account Registration</h1>			{if $message != ""}				<p>{$message}</p>			{else}				<p>				Sign up here to claim one of the nicknames used in the irc chat logs as your own.<br />				To do so you must provide a reference by selecting someone that's already registered in				this system that can vouch for you really being who you say you are.				</p>				<p>Once you've signed up and claimed a nickname, you'll be able to tag posts, save				searches and a variety of other mundane things.				</p>					{/if}				<div class="input">					<label for="user">						Nickname					</label>					<input type="text" name="user" value="{$user.user}" />				</div>				<div class="input">					<label for="email">						Email					</label>					<input type="text" name="email" value="{$user.email}" />				</div>				<div class="input">					<label for="reference">						Reference					</label>					<select name="reference">						{$known_users|@var_dump}						{foreach from=$known_users item="row" key="index"}							<option value="{$row.id}" {if $reference == $row.id}selected="selected"{/if}>{$row.user}</option>						{/foreach}					</select>				</div>				<div class="input">					<label for="password">						Password					</label>					<input type="password" name="password" />				</div>				<div class="input">						<label>&nbsp;</label>					<input type="submit" value="Register" />				</div>		{elseif $step == 2}			{if $sendmail}				<p>And now... we wait.</p>			{else}				<p>There was a problem sending the email for authentication. Please contact the system administrator.</p>				{$mail}			{/if}		{/if}	</form></div>